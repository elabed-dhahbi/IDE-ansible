---
# DRS Installation and Configuration

- name: Include DRS installation tasks
  include_tasks: install_drs.yml
  tags: [drs, install]

# RPM creates the drs user and /opt/drs layout. Only verify, do not recreate.
- name: Include DRS user configuration tasks
  include_tasks: configure_user.yml
  tags: [drs, user]

- name: Include DRS directory verification tasks
  include_tasks: configure_directories.yml
  tags: [drs, directories]

- name: Include DRS configuration tasks
  include_tasks: configure_drs.yml
  tags: [drs, config]

- name: Include DRS service configuration tasks
  include_tasks: configure_service.yml
  tags: [drs, service]
