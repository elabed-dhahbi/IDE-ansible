---
# OpenSearch Dashboards System Package Installation

- name: Install required system packages for OpenSearch Dashboards
  yum:
    name:
      - wget
      - curl
      - unzip
    state: present
  become: yes
  tags: [opensearch_dashboards, system, packages]








